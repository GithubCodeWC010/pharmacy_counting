This code reads the data, i.e., the list of customers and the prescription
drugs and their prices for each one, from the input file "\input\input_data_pres_drug"
and sorts the total price over all customers for each drug in a descending order At the
end the results of the sorting are saved into "\output\output_top_cost_drug.txt".